# Tests — rag-chatbot

Unit and integration tests for the rag-chatbot module.

Naming convention: mirror the source file structure, e.g. a test for `services/scheduling.py` lives at `tests/services/test_scheduling.py` (adjust to your language/framework's convention).

Run via CI on every pull request into `main` (see `.github/workflows/ci.yml`).
